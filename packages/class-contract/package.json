{
  "name": "@moderno-ui/class-contract",
  "version": "0.1.0",
  "description": "Moderno class contract — the single source for md-* class names, variant builders, and the legal-name set the CSS generator enforces.",
  "type": "module",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "tsup": "^8.3.0"
  }
}
